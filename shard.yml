name: dirless-cli
version: 0.1.0

description: CLI for enrolling and managing Dirless identity nodes

authors:
  - Dirless <hello@dirless.io>

license: Private

crystal: ">= 1.9.0"

dependencies:
  age-crystal:
    path: /src/age-crystal

  x509-crystal:
    path: /src/x509-crystal

development_dependencies:
  ameba:
    github: crystal-ameba/ameba

  webmock:
    github: manastech/webmock.cr
    branch: master
